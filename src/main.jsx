import React from "react";
import ReactDOM from "react-dom/client";
import SmartCalculator from "./smart-calculator.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SmartCalculator />
  </React.StrictMode>
);
